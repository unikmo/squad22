import type { Metadata } from "next";
import { PublicPage } from "../lib/public-page";
export const metadata: Metadata = { title: "Prescription Cash Price FAQ", description: "Answers about IPNUS cash prices, reservations, prescriptions, and independent pharmacies." };
const faqs = [["What is IPNUS?", "IPNUS is a prescription cash-price search platform for participating independent pharmacies."], ["Are these coupon prices?", "No. IPNUS is built around published pharmacy cash prices, not coupon cards."], ["Are prices guaranteed?", "Prices are published by participating pharmacies and may require confirmation before fulfillment."], ["Do I still need a prescription?", "Yes. Prescription requirements still apply."], ["Can I reserve a price?", "You can send a reservation request. The pharmacy confirms availability, prescription requirements, and pickup or delivery options."], ["Why independent pharmacies?", "Independent pharmacies can often provide direct service, local support, and transparent cash pricing without sending patients through coupon-card confusion."]];
export default function FAQPage() { return <PublicPage><div className="mx-auto max-w-4xl px-6 py-16"><h1 className="text-5xl font-black tracking-tight text-slate-950">Prescription cash price FAQ</h1><div className="mt-12 divide-y divide-slate-200 border-y border-slate-200">{faqs.map(([question, answer]) => <section key={question} className="py-7"><h2 className="text-xl font-black">{question}</h2><p className="mt-2 leading-7 text-slate-600">{answer}</p></section>)}</div></div></PublicPage>; }
