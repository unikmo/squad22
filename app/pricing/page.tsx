import type { Metadata } from "next";
import Link from "next/link";
import { PublicPage } from "../lib/public-page";
export const metadata: Metadata = { title: "Pharmacy Pricing", description: "Simple launch pricing for participating independent pharmacies." };
export default function PricingPage() { return <PublicPage><div className="mx-auto max-w-5xl px-6 py-16"><h1 className="text-5xl font-black tracking-tight text-slate-950">Simple pricing for independent pharmacies.</h1><p className="mt-5 max-w-3xl text-xl text-slate-600">Publish cash prices, receive reservation requests, and compete for local patients without coupon-card middlemen.</p><div className="mt-12 grid gap-6 md:grid-cols-2"><Plan title="Founding Pharmacy" price="6 months free" copy="For early independent pharmacies" items={["Claimed profile", "Published cash prices", "Reservation requests", "Basic pharmacy dashboard"]} cta="Apply as Founding Pharmacy" href="/contact" featured /><Plan title="Standard" price="Coming soon" copy="For participating pharmacies after founding launch" items={["Transparent pharmacy profile", "Cash-price publishing", "Reservation requests", "Launch support"]} cta="Contact IPNUS" href="/contact" /></div></div></PublicPage>; }
function Plan({ title, price, copy, items, cta, href, featured = false }: { title: string; price: string; copy: string; items: string[]; cta: string; href: string; featured?: boolean }) { return <section className={`rounded-3xl border p-8 ${featured ? "border-emerald-600 bg-emerald-50" : "border-slate-200"}`}><h2 className="text-2xl font-black">{title}</h2><p className="mt-4 text-3xl font-black text-emerald-800">{price}</p><p className="mt-2 text-slate-600">{copy}</p><ul className="mt-6 grid gap-3 text-slate-700">{items.map((item) => <li key={item}>✓ {item}</li>)}</ul><Link href={href} className="mt-8 inline-block rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white">{cta}</Link></section>; }
