# TODO.md

## Phase 2.1 – DrugPrice Prisma Model
- [ ] Update `prisma/schema.prisma` with `DrugPrice` model + `Pharmacy.drugPrices` relation, indexes, and unique constraint.
- [ ] Run `npx prisma migrate dev --name phase2_drug_price_model`.
- [ ] Run `npx prisma generate`.
- [ ] Run `npm run smoke:ipn`.
- [ ] Run `npm run lint`.
- [ ] Run `npm run build`.

